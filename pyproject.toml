[project]
name = "src"
version = "1.0.0"
description = "Automated BibTeX generation and metadata enrichment tool"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[project.optional-dependencies]
dev = []